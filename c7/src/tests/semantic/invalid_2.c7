int main(float z) {         // main: 0, z: 1
    if (da) {               // da: raises error
        int ea;             // ea: 2
        int ab;
    } else if (ab) {        // ab: raises error
        return var;         // var: raises error
    } else {
        write("something");
    }
}
