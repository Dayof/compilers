int test_for () {
    for (i = 1; i < 11; i = i + 1) {
        float a;
        if(subsum(s, target, cur_sum, ans)) return ans;
        add(el in s);
    }

    for(count = 1; count <= num; count = count + 1) return ans;
    for (num=10; num<20; ) return ans;
    for (;num<20;) return ans_2;

    int num;
    for (;num<20;num = num + 1) float b;

    for (int i=0; i<2; i = i + 1) {
        for (int j=0; j<4; j = j + 1) {
            return 0;
        }   
    }
}